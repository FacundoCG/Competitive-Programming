#include <bits/stdc++.h>

using namespace std;

void towerHanoi(int n) {
    int k = pow(2,n)-1;

    cout << k << "\n";

    for (int i=1; i<k+1; i++){

    }
}

int main() {
    ios :: sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin >> n;

    towerHanoi(n);
}
